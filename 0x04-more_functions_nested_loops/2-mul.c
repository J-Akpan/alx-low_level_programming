#include "main.h"

/**
 * Description: mul - program to multultiply numbers
 * @a: paramater to multiply
 * @b: second parameter to be checked
 * Return: 0 always
 */

int mul(int a, int b)
{
	int mult;

	mult = a * b;
	return (mult);
}
